@model Bibliotek.Models.ViewModels.LoanIndexVM

@{
    ViewData["Title"] = "Index";
}

<h2>Lån</h2>

<p>
    <a asp-action="Create">Registrera Lån</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Loans.FirstOrDefault().Member.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Members.FirstOrDefault().FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loans.FirstOrDefault().BookID)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Loans)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemberID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemberID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
