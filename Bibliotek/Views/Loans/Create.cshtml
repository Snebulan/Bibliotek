@model Bibliotek.Models.Loan

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Loan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div>
                <label asp-for="Member" class="control-label"></label>
                <select asp-for="Member.ID" asp-items="ViewBag.Members" class="form-control"></select>
            </div>
            <div>
                <label asp-for="Book" class="control-label"></label>
                <select asp-for="Book.ID" asp-items="ViewBag.Books" class="form-control"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
